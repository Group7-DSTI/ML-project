class FilePaths:
    """
    Class containing static file paths for training, testing, validation, and validation submission files.
    
    Attributes:
        train_file_path (str): File path for the training set.
        test_file_path (str): File path for the test set.
        valid_file_path (str): File path for the validation set.
        valid_sub_file_path (str): File path for the validation sample submission.
        training_data (str) : File path for EDA
    """
    train_file_path = "training_set_rel3.tsv"
    test_file_path = "test_set.tsv"
    valid_file_path = "valid_set.tsv"
    valid_sub_file_path = "valid_sample_submission_2_column.csv"
    training_data = "trainingdata01.csv"

